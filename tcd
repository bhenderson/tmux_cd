#!/bin/bash

set -euo pipefail

name=${1-}

if [ -z "$name" ]; then
	tmux list-sessions
	exit 0
fi

if [ -d "$name" ]; then
	tmux new-session -A -c "$name" -s "$(basename "$name")"
	exit 0
fi

tmux attach-session -t "$name"
